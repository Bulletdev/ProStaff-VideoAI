services:
  api:
    build: .
    container_name: prostaff-videoai
    env_file: .env
    volumes:
      - .:/app
      - clips_data:/tmp/videoai_clips
    ports:
      - "8002:8001"

volumes:
  clips_data:
